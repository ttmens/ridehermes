# RideHermes Backend Environment Variables
# Copy to .env and adjust for your environment
# 所有敏感配置必须通过环境变量设置，不要写入配置文件

# Database
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=ridehermes123
DB_NAME=ride_hermes

# Redis
REDIS_ADDR=localhost:6379
REDIS_PASSWORD=

# JWT (生产环境必须设置强密码，至少32字符)
JWT_SECRET=your-super-secret-jwt-key-change-me-in-production

# AI Service
AI_SERVICE_ADDR=http://localhost:8001

# 高德地图 API
AMAP_API_KEY=473c3c82dc0cd2f3ecf99eb3d0babeaa

# Server
SERVER_PORT=8080
GIN_MODE=debug  # 生产环境设置为 release
